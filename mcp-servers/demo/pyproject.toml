[project]
name = "cts-mcp-demo"
version = "0.1.0"
description = "Sample MCP server for CTS demo"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
